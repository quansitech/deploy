{
    "name": "webhook",
    "version": "1.0.0",
    "description": "webhook for docker",
    "main": "app.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
      "webhook"
    ],
    "dependencies": {
      "express": "^4.14.0"
    }
}